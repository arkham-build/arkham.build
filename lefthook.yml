pre-commit:
  commands:
    check:
      glob: "*.{js,ts,cjs,mjs,d.cts,d.mts,jsx,tsx,json,jsonc,css}"
      run: npx oxlint --fix --no-error-on-unmatched-pattern {staged_files} && npx oxfmt --write --no-error-on-unmatched-pattern {staged_files} && git update-index --again
